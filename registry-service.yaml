server:
  port: 8099

eureka:
  instance:
    hostname: localhost
  client:
  fetchRegistry: false
  registryWithEureka: false
  serviceUrl:
    defaultZone: http://${eureka.instance.hostname}:${server.port}
